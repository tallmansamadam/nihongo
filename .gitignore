node_modules
dist
*.local
.DS_Store

# kuromoji dictionary — regenerated from node_modules by `npm run prepare-dict` (postinstall)
public/dict

# Tauri (desktop/mobile) build artifacts
src-tauri/target
src-tauri/gen

# Built Android APKs
*.apk

# Bundled content snapshot — regenerated by scripts/sync-content.mjs on build
public/content

# resumable kanji fetch cache (build artifact)
scripts/.kanji-dict-cache.json
